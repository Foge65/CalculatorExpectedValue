services:
  backend:
    build: spring
    ports:
      - "8084:8084"

  frontend:
    build: web
    ports:
      - "8085:8085"
    depends_on:
      - backend
    volumes:
      - /etc/nginx/sites-enabled/calc.firestorm.team:/etc/nginx/nginx.conf:ro
